Config(
  x: Fraction(0.5),
  y: Fraction(0.35),
  width: Absolute(600),
  close_on_click: true,
  hide_plugin_info: true,
  plugins: [
    "libshell.so",
    "libapplications.so",
    "librink.so",
    "libwebsearch.so",
  ],
)
